<h1><%= @article.title %></h1> <%= link_to "Edit", edit_path %> | <%= link_to "Delete", article_path(@article), method: :delete, data: { confirm: "Are you sure you want to delete this?" } %>

<p><%= @article.content %></p>

<%= link_to "Back to all articles", articles_path %>